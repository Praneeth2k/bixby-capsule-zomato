action (findTimings) {
  type(Search)
  description (Find the timings of a restaurant)
  collect {
    input (restaurantName) {
      type (restaurantName)
      min (Required) max (One)
    }
    input (area){
      type(area)
      min (Required) max (One)
    }
  }
  output (timings)
}